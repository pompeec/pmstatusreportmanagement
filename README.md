# PM Status Report Management

## AI-Powered TPM Skill

An AI-powered TPM skill that converts raw program signals (Jira, defects, OKRs, meetings) into an executive-ready status update with clear health indicators, risks, and decisions.

## Quickstart

```bash
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt
streamlit run app.py